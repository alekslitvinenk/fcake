var doc = fl.getDocumentDOM();var selects = doc.selection;for(var i = 0; i < selects.length; i++){	var curItem = selects[i];		if(curItem.parameters){				for(var j = 0; j < curItem.parameters.length; j ++){						var curParam = curItem.parameters[j];						if(curParam.valueType == "List"){				fl.trace(curItem.name + "." + curParam.name + " = \"" + curParam.value[curParam.listIndex].value + "\"; //valueType:" + curParam.value[curParam.listIndex].valueType);			}else if(curParam.valueType == "Color"){				fl.trace(curItem.name + "." + curParam.name + " = " + "0x" + curParam.value.substr(1) + "; //valueType:" + curParam.valueType);			}else if(curParam.valueType == "String"){				fl.trace(curItem.name + "." + curParam.name + " = \"" + curParam.value + "\"; //valueType:" + curParam.valueType);			}else{				fl.trace(curItem.name + "." + curParam.name + " = " + curParam.value + "; //valueType:" + curParam.valueType);			}		}				fl.trace("");	}}