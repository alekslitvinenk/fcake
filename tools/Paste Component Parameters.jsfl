var doc = fl.getDocumentDOM();var selects = doc.selection;if(selects.length == 1){		var targetComponent = selects[0];		if(targetComponent.parameters)	{				if(doc.sourceComponent)		{						for(var i = 0; i < currentItem.parameters.length; i++){				targetComponent.parameters[i].value = doc.sourceComponent.parameters[i].value;			}						alert("Parameters were updated!");					}else		{			alert("Nothigs was copied yet!");		}			}else	{		alert("Selected element has no properties!");	}	}else{	alert("Please selcet only one element!");}